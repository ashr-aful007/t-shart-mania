/**
 * Basic Router Setup:
 * 1. npm i react-router-dom
 * 2. create a router using createBrowserRouter
 * 3. Create a lot of components
 * 5. create Main layout with some common part and one changing part base on the route
 * 6. set main layout at the root of the route
 * 7. set children routs 
 * 8. set links on the header components  
 */

/**
 * context Api
 * 1. declare a context
 * 2. set provider with value
 * 3. useContext in the place where you want to get the conteext
 */